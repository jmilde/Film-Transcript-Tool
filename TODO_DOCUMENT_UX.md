# Document Builder UX Consistency Overhaul — Implementation TODO

Persistent, git-tracked checklist for making the Document Builder feature
(`TODO_DOCUMENT_BUILDER.md`, phases D1–D8 done) consistent with the existing
transcript-editing UX: resizable panels, highlight-and-comment in document
prose, a shared "what can I do with this selection" popup, an
insertion-point marker settable from the document that survives navigating
back to the transcript, link-preserving copy/paste from transcript into
document, a uniform video player, and unified comments across transcript
and document. Stands alongside `TODO.md`, `TODO_SEMANTIC_SEARCH.md`, and
`TODO_DOCUMENT_BUILDER.md` (read `backend/CLAUDE.md` and the repo-root
`CLAUDE.md` first — same rules apply: fully typed, `mypy --strict` clean,
ruff clean, TDD, test tree mirrors source tree, explicit imports, no
`__all__`, autogenerated migrations only, `make check` green at the end of
every backend phase).

Design record: `/Users/jan/.claude/plans/i-want-to-improve-witty-glacier.md`
— read it first for full rationale on every decision below; this checklist
is the concrete execution of that plan.

Confirmed decisions (see the design record for the "why" behind each — do
not re-litigate these while implementing):
- **Comments: fully unified.** One `Comment` entity with a polymorphic
  anchor — either a transcript token range (existing `CommentRange`) or a
  document anchor (a TipTap `comment` mark on a run of prose text, or a
  `clipBlock` node's stable `nodeId`). One shared comments UI renders both,
  filtered to whichever context is active.
- **Panels: merged.** The document panel becomes a true resizable peer of
  the routed page inside one `react-resizable-panels` `Group` owned by
  `AppShell` (not a separate fixed-width sidebar). `VideoWorkspace` keeps
  its own nested transcript/video `Group` inside that outer page panel.
- **Editor:** keep TipTap WYSIWYG with markdown-style input shortcuts (no
  raw-markdown-source-view toggle). Fix that `# `/`## ` don't currently
  render as visually distinct headings (missing `@tailwindcss/typography`
  plugin registration). Add a real selection/formatting toolbar
  (bold/italic/heading levels) rather than relying on typed shortcuts
  alone — this toolbar is the same shared component used for the
  transcript's selection actions and the document's comment/clip popup.
- **Clip references: inline text, not cards.** `clipBlock` changes from a
  block-level `atom` node (rendered as a thumbnail/timecode card) to an
  **inline** `atom` node — still non-editable/read-only, excerpt still
  resolved fresh server-side, never persisted — rendered as styled inline
  text within the document's normal paragraph flow, not a boxed card.
- **Clip vs. comment visual channels don't collide.** A persistent left
  border + subtle background tint marks "this text is from source
  material" (clips); the underline decoration (violet = unresolved,
  slate = resolved, exactly as in the transcript today) is reserved
  exclusively for comments, in both transcript and document. The two
  layers stack independently — a clip with a comment on it shows both at
  once.
- **Clip "note" field is retired.** Per-clip notes fold into the unified
  comment system instead of staying a separate always-visible input — a
  note becomes a normal `Comment` row anchored via
  `DocumentCommentAnchor.clip_node_id`.
- **Insertion-point marker is a live ProseMirror plugin position, not a
  stored offset.** Never persist a raw position integer anywhere (Zustand
  included) — it goes stale on the next edit. The marker lives in the
  editor instance's own plugin state, mapped through every transaction
  exactly like the comment mark; it survives navigating to the transcript
  because `DocumentPanel`/`DocumentEditor` are already mounted persistently
  in `AppShell` and never unmount on route/focus changes.
- **Clipboard interop uses `text/html` + `text/plain`, not a custom MIME
  type** (custom `ClipboardItem` types have inconsistent cross-browser
  safelisting; `text/html`/`text/plain` are universal and `text/plain` stays
  byte-for-byte identical to today's behavior, so external paste is
  unaffected).

---

## Phase E1 — Heading typography fix
- [x] Add `@tailwindcss/typography` to `frontend/package.json` devDependencies
- [x] Add `@plugin '@tailwindcss/typography';` to `frontend/src/index.css`
- [x] Extend `DocumentEditor.test.tsx`: typing `# `/`## ` produces an `h1`/`h2` element with the typography plugin's classes applied
- [ ] Verify manually: type `# Heading` in a document, confirm it renders visually distinct from body text; check list/paragraph spacing doesn't fight the panel's existing padding

## Phase E2 — Backend: document-anchored comments schema
- [x] `backend/app/models/comment.py`: make `Comment.transcript_id` nullable; add `Comment.document_id` (FK `documents.id`, `ondelete="CASCADE"`, indexed, nullable); add a `CheckConstraint` enforcing exactly one of `transcript_id`/`document_id` is set
- [x] Add new model `DocumentCommentAnchor(Base, UUIDPrimaryKeyMixin, CreatedAtMixin)`: `comment_id` (FK `comments.id`, unique, indexed), `clip_node_id: str | None` (set when anchored to a `clipBlock` node's stable `nodeId`; `None` when anchored to a run of prose text, whose real anchor is the TipTap `comment` mark added in Phase E4)
- [x] Migration: `cd backend && uv run alembic revision --autogenerate -m "0014 document comments"` — verify the `CheckConstraint` came through correctly (autogenerate sometimes needs a manual nudge for check constraints; fix the model/naming convention and regenerate rather than hand-patching DDL, per `backend/CLAUDE.md`)
- [x] Verify `alembic upgrade head` / `alembic downgrade -1` round-trip cleanly; no backfill needed (widening an existing non-null column to nullable loses no data)
- [x] `backend/app/schemas/comment.py`: split `CommentRead.anchor` into a discriminated union — `TranscriptCommentAnchor{kind: "transcript", transcript_id, start_token_id, end_token_id, in_time, out_time}` vs `DocumentCommentAnchorRead{kind: "document", document_id, clip_node_id, excerpt}`. Add `DocumentCommentCreate{clip_node_id: str | None, text: str}` (prose-text comments additionally need the frontend to apply the `comment` mark — see E4; `clip_node_id` set means no mark involved)
- [x] `backend/app/services/comments.py`: add `create_document_comment(session, document, *, clip_node_id, text, user_id) -> Comment` (mirrors `create_comment`; sets `document_id` + creates the `DocumentCommentAnchor` row; does **not** touch `document.content`)
- [x] `backend/app/services/documents.py`: add a helper resolving a document-anchored comment's excerpt at read time — reuse/generalize the existing content-tree-walking helper(s) behind `resolve_document_content` to also find `comment`-marked text spans (once E4 adds the mark) in addition to `clipBlock` nodes
- [x] `backend/app/api/routes/comments.py`: branch `_comment_read` on `comment.transcript_id is not None` vs `document_id is not None`; add `POST /documents/{document_id}/comments` (`require_min_role(require_document_access, MembershipRole.EDITOR)` — match the existing editor-floor pattern used for transcript comment creation) and `GET /documents/{document_id}/comments` (`require_document_access`, viewer floor)
- [x] Tests first, mirroring source tree: `tests/models/test_models.py` (nullable `transcript_id` + `document_id` construction, check-constraint violation case), `tests/services/test_comments.py` (new `create_document_comment` cases), `tests/api/routes/test_comments.py` (new document-comment route tests; **confirm every existing transcript-comment test still passes unchanged** — regression guard for the nullable-column change)
- [x] Verify: `make check` green (two unrelated pre-existing/flaky failures confirmed independent of this change: `test_build_document_resolves_speaker_name` fails identically on the pre-E2 base commit; `test_export_lifecycle`'s Postgres connection drop passes on rerun)

## Phase E3 — Frontend: unified comments data layer
- [x] Regenerate API client types (`npm run gen:api` or equivalent) against the new discriminated `CommentRead`
- [x] `frontend/src/api/hooks/useComments.ts`: add `useDocumentComments(documentId)`, `useCreateDocumentComment(documentId)`
- [x] Verify: `typecheck`/`lint` green; hook tests (MSW-backed, mirroring existing `useComments` test conventions)

## Phase E4 — Clip references as inline text + TipTap comment mark
- [x] `frontend/src/features/documents/clipBlockNode.ts`: change `group: 'block'` → `group: 'inline', inline: true` (keep `atom: true`, `selectable: true`, `draggable: false`, same name/attrs/`insertClipBlockAt` command — no backend change needed, `resolve_document_content`'s tree-walk doesn't care about block-vs-inline); move `parseHTML`/`renderHTML` from `div[data-clip-block]` to `span[data-clip-block]`. Also fixed a latent pre-existing bug found while doing this: resolved-only display fields (`excerpt`, `video_name`, etc.) were never declared as node attrs, so ProseMirror's schema silently dropped them on every `setContent`/`insertContentAt` — they now round-trip into `node.attrs` (still stripped before save by `stripResolvedClipFields`). Also fixed `insertClipBlockAt` inserting at `doc.content.size`, which resolves to a block boundary (parent `doc`) for *any* non-empty document, not just an empty one — it now steps back into the preceding textblock when one exists, only wrapping a fresh paragraph when there truly isn't one
- [x] Remove the `note` attribute from `ClipBlockAttrs`
- [x] Rewrite `frontend/src/features/documents/ClipBlockView.tsx` from a card layout to an inline `<span>`: render the resolved `excerpt` text itself as the visible content, styled with a persistent left-border + background tint (see decision above); drop the thumbnail/video-name/folder-path row from the inline rendering; Play/Comment/Remove render as a minimal action row on selection (interim trigger — Phase E6 replaces this with the shared popup)
- [x] Ensure inline-atom insertion always lands inside a paragraph (an inline atom can't be a direct child of `doc`): update `insertClipBlockAt`'s call sites (the pending-insert consumption effect in `DocumentEditor.tsx`, and Phase E7's marker-target insert) to wrap in a new empty paragraph when the target position is a block boundary (e.g. end of an empty document)
- [x] New `frontend/src/features/documents/commentMark.ts`: TipTap `Mark` (not `Node`) named `comment`, carrying only `commentId` as an attribute (never `resolved`/`text` — those stay authoritative on the `Comment` row; the mark is purely a stable position reference riding ProseMirror's transaction mapping through edits/undo)
- [x] `DocumentEditor.tsx`: register `CommentMark`; wire "select text → Comment action" (via an interim always-visible Comment button — Phase E6 wires the same underlying function to the shared `BubbleMenu`/`SelectionToolbar` instead) to (1) POST via E3's `useCreateDocumentComment` hook, (2) on success, `setMark('comment', { commentId })` — flows through the existing debounced-autosave/`expected_version` path. On a 409 immediately following the mark-set, retries the mark-set once against freshly-reloaded content; if that also fails, falls back to the normal conflict banner (no further auto-retry loop) — the comment row still lists/functions regardless. Resolved/unresolved styling is a ProseMirror decoration plugin (`commentResolvedDecoration.ts`) driven by a `setMeta` transaction, not a DOM attribute poked in after render — PM would silently wipe the latter the next time it redraws a mark span from `renderHTML`
- [x] Clip-anchored comments (including former "notes") skip the mark entirely: write `DocumentCommentAnchor.clip_node_id` directly from the clip node's existing stable `nodeId` attr, no document edit needed
- [x] Add a delegated click handler on `[data-comment-id]` spans (at the `EditorContent` container) that selects the comment in the shared `useCommentsStore` — clip nodes get a NodeSelection natively from `selectable: true`. Note: nothing renders a document-scoped comments list yet, so this only sets state today; Phase E6's popup is what will actually consume it — don't assume "open the comment thread/popup" is fully wired until then
- [x] Compute resolved/unresolved styling (underline for comments; border+tint for clips) by diffing marks/nodes present in the doc against the loaded comments list's `resolved` field — never store `resolved` redundantly on the mark/node itself
- [x] Tests: comment created from a selection survives a subsequent unrelated edit at an earlier position (position-mapping regression guard); the 409-then-conflict-banner case; a clip renders with border+tint only, gains an underline once a comment is added, both survive a reload (simulated via unmount+remount); inline-insert-into-empty-document paragraph-wrapping case (plus end-of-prose and mid-sentence cases, which caught the `content.size` bug above)
- [ ] Verify manually: select prose text, add a comment, reload — highlight and thread persist; edit text before the comment — mark stays attached to the right words (deferred to a consolidated manual pass after Phase E9, once the panel is properly integrated into `AppShell`)

## Phase E5 — Extract shared `SelectionToolbar`
- [x] New `frontend/src/features/toolbar/SelectionToolbar.tsx` + `ToolbarAction` type: `{id, icon, label, variant?, active?, onClick}[]`, a `summary: {text, timecode?}` prop, and support for the existing "swap to inline draft input" mode (the Comment button's type-then-Confirm/Cancel flow)
- [x] Extract `TranscriptViewer.tsx`'s existing inline toolbar block (Play/Copy/Edit/Comment/Add to Document/Delete) into this component — pure extraction, zero behavior change; `TranscriptViewer` keeps its existing token-selection state and action implementations, just builds a `ToolbarAction[]` and renders `<SelectionToolbar>`
- [x] Move/adapt existing toolbar-behavior tests — added new `SelectionToolbar.test.tsx` unit tests (summary/actions, clear button, draft mode Confirm/Cancel/Enter/Escape); `TranscriptViewer.test.tsx`'s existing integration tests needed zero changes
- [x] Verify: `TranscriptViewer` behavior unchanged (existing 27 tests pass byte-for-byte unchanged)

## Phase E6 — Document formatting/comment `BubbleMenu`
- [x] Add `@tiptap/extension-bubble-menu` as a direct dependency in `frontend/package.json`
- [x] `DocumentEditor.tsx`: use TipTap's `BubbleMenu` (from `@tiptap/react/menus`) to render `SelectionToolbar`, with `shouldShowBubble` (new `shouldShowBubble.ts`, split out of `DocumentEditor.tsx` to avoid a `react-refresh`/`only-export-components` lint warning on a component file) distinguishing a text selection (→ Bold/Italic/H1/H2/BulletList/Comment, via a new `useEditorState` selector `bubbleSelection`) from a `NodeSelection` over a `clipBlock` node (→ Play/Comment/Remove, via a `playClip` helper moved from `ClipBlockView`'s old play-routing logic). Removes E4's interim always-visible Comment button and `ClipBlockView`'s inline selected-action-row entirely — `ClipBlockView` now only renders the excerpt + decoration classes; the node still gets a native ProseMirror `NodeSelection` for free from `selectable: true` on click, no extra wiring needed. `documentCommentsContext.ts`'s `createClipComment` was removed (comment creation now flows through the shared popup at the `DocumentEditor` level for both text and clip targets, via a unified `CommentDraftTarget` union) — the context now only carries `clipCommentStatus` for underline styling.
- [x] Wire the same popup to open on a click on a clip's inline span — satisfied natively: `selectable: true` gives the clip node a real `NodeSelection` on click, which `shouldShowBubble` recognizes. A click on a `[data-comment-id]` span intentionally keeps E4's existing behavior (`selectComment` → the shared `CommentsPanel` thread via `useCommentsStore`), not the `BubbleMenu` — a comment *mark* produces no ProseMirror selection for `shouldShowBubble` to key off, and E4 already decided "one shared comments UI for both contexts" (viewing/replying to an existing comment's thread is a different action than "what can I do with this selection")
- [ ] Verify manually: select plain prose text → bubble shows Bold/Italic/H1/H2/Comment; select an inline clip → bubble shows Play/Comment/Remove (deferred to the consolidated manual pass after Phase E9, per E4's note)
  - Automated coverage note: the text-selection bubble (Bold/Italic/H1/H2/BulletList/Comment, including toggling Bold) is covered by a `DocumentEditor.test.tsx` integration test. The clip-`NodeSelection` bubble (Play/Comment/Remove) is **not** covered by an integration test — ProseMirror's real click-to-select-a-node resolves the position via `view.posAtCoords` (on-screen geometry jsdom doesn't provide), and its DOM-selection-read fallback is deliberately disabled for React node views (`ReactNodeView.ignoreMutation` always returns `true`, so React's own DOM updates inside a node view are never misread as document mutations) — so neither path is reachable in jsdom without adding an editor-access seam to production code purely for tests, which isn't justified for one manually-verified branch. `shouldShowBubble`'s text-vs-clip discrimination itself (the part of this phase most worth unit testing) is covered directly in `shouldShowBubble.test.ts` by constructing `TextSelection`/`NodeSelection` states against a headless `Editor` instance.

## Phase E7 — Insertion-point marker
- [x] New `frontend/src/features/documents/insertMarker.ts`: ProseMirror plugin (`InsertMarker` extension, keyed by `insertMarkerPluginKey`) holding the marker position in plugin state, mapped through every transaction (`tr.mapping.map`) unless overridden by a `setInsertMarker`/`clearInsertMarker` meta; emits a `Decoration.widget` (thin `bg-sky-500` bar, `data-insert-marker`) at the mapped position
- [x] `frontend/src/store/documentPanel.ts`: added `insertMarkerDocumentId: string | null` + `setInsertMarkerDocumentId` setter (thin flag only, not a position — the position lives solely in the plugin state above); `setActiveDocument` now always resets it to `null`, since switching documents tears down and rebuilds the editor (a fresh plugin instance starts at `null` regardless)
- [x] `DocumentEditor.tsx`: registers `InsertMarker`; a status row under the title shows a `PinIcon` (new, `icons.tsx`) plus "Mark insert point here" (sets the marker at `editor.state.selection.to`, mirrored into the store flag via a `useEditorState` selector on the plugin's own state) or, once set, "Insert point set" + "Clear insert point". The pending-insert consumption effect now reads `insertMarkerPluginKey.getState(editor.state)` **inside `resolveClipBlock`'s `onSuccess`** (not before the mutate call — `resolveClipBlock` is a network round-trip, and reading the position before it would let an edit landing during that window map the plugin's own state forward correctly while leaving an earlier-captured `targetPos` stale, which is exactly the "raw integer goes stale" failure this phase exists to avoid), falling back to `doc.content.size` when unset. Advances the marker to `editor.state.selection.to` right after `insertClipBlockAt` runs (whose own `insertContentAt` calls already leave the selection just past the inserted node in every branch — empty-doc wrap, end-of-prose, and mid-sentence) whenever a marker was in effect, rather than clearing it — so repeated inserts land in order at the marked spot without re-marking each time
- [x] Tests: `insertMarker.test.ts` unit-tests the plugin directly (starts `null`; sets/reads back; maps a set position forward through an unrelated earlier edit by exactly the inserted length; leaves a position unaffected by an edit further down the document; clears). `DocumentEditor.test.tsx`'s new `insertion-point marker` describe block covers landing a queued insert at the marked position and advancing past it over two successive inserts, and clearing the marker to fall back to end-of-document
- [ ] Verify manually: mark an insert point in the document, click into the transcript, select a range, click "Add to Document" — confirm it lands at the marked position, not document end; confirm the marker advances on repeated inserts (deferred to the consolidated manual pass after Phase E9, per E4's/E6's note)
  - Automated coverage note: "an intervening unrelated edit correctly re-maps the marker" is proven only at the plugin level (`insertMarker.test.ts`), not end-to-end through `DocumentEditor`. An end-to-end version needs a DOM-selection edit *after* the marker's widget decoration has already split the paragraph's text around a zero-width sibling node, using the same jsdom DOM-selection-simulation technique (`selectWithinText`) E6 already documented as dependent on real click/focus geometry jsdom doesn't provide; combined with a defocusing button click in between, the simulated edit landed at different wrong offsets from run to run instead of exercising `tr.mapping` at all — an order-dependent harness artifact, not a reflection of the marker logic (confirmed: the failures were in the edit-simulation *setup* step, before the mapping assertion ever ran). Reading the marker fresh inside `onSuccess` (the specific stale-read bug this phase's fix addresses) is similarly not covered by a race-inducing integration test — a gated-response version of the same test hit the identical simulation flakiness — but is covered by construction: the happy-path test exercises the same `onSuccess` read, and the mapping-through-an-edit behavior it depends on is proven deterministically at the plugin level.

## Phase E8 — Clipboard interop (link-preserving copy/paste)
- [x] `TranscriptViewer.tsx`'s Copy action (and a new equivalent for a document text/clip selection): write a `ClipboardItem` with `text/plain` unchanged (today's excerpt string) and `text/html` containing a marker element using the `data-clip-block`/`data-transcript-id`/`data-video-id`/etc. attributes `clipBlockNode.ts` already defines for its `parseHTML` (now `span[data-clip-block]` per E4), with the excerpt as the span's visible inner text. Feature-detect `ClipboardItem`/`clipboard.write`, fall back to today's `writeText` when unavailable
  - New shared module `frontend/src/features/documents/clipClipboard.ts`: `clipBlockMarkerHtml(attrs, excerpt)` builds the marker span, and `writeClipToClipboard(text, html)` does the `ClipboardItem`/`clipboard.write` feature-detection with a `writeText` fallback.
  - Empirical finding on attribute naming: TipTap's default attribute *parsing* (`injectExtensionAttributesToParseRule` in `@tiptap/core`) reads `element.getAttribute(<attrName>)` **verbatim** — it does not dasherize `data-*`-style. So the marker span's attrs are plain (un-prefixed) `nodeId`/`transcriptId`/`videoId`/`startTokenId`/`endTokenId`, matching `ClipBlockAttrs`'s keys exactly, not `data-transcript-id` etc. as the phase's own description assumed — confirmed by reading the library source and by the round-trip test below, which fails immediately if the names don't match.
  - `TranscriptViewer.tsx`: `copySelection()` replaces the old bare `writeText` Copy action, generating a fresh `nodeId` (`crypto.randomUUID()`) for the clip at copy time (mirrors how `DocumentEditor` mints one at insert time).
  - `DocumentEditor.tsx`: added a `Copy` action to the text-mode bubble menu (`copyTextSelection`) — there was no copy affordance in the document editor before this phase. Built from the schema's own `DOMSerializer` (`@tiptap/pm/model`) over `editor.state.doc.slice(from, to)`, so any inline `clipBlock` node embedded in the selection serializes through its own `renderHTML` for free — no bespoke HTML-building for the document side, unlike `TranscriptViewer`'s token-selection (which isn't a ProseMirror selection at all, so it has no schema to serialize through). The plain-text entry uses `textBetween(from, to, ' ', leafText)` with a `leafText` callback that substitutes a clip node's `excerpt` attr, since an atom node otherwise contributes nothing to the default text join.
  - Real bug caught by review before committing: `clipBlockMarkerHtml` originally put the excerpt only as the span's *inner text*, not as an attribute. Since `ClipBlock` is `atom: true` (no content spec), ProseMirror drops a parsed atom's inner text entirely, and nothing re-resolves a pasted node's excerpt during the session — `DocumentEditor` only calls `resolveClipBlock` for its own queued-insert path — so a transcript-copied-and-pasted clip would silently render the bare "Clip" placeholder until the whole document reloaded. Fixed by also carrying `excerpt` as a plain attribute (it's already declared on `ClipBlock` via `RESOLVED_ONLY_KEYS` and stripped on save by `stripResolvedClipFields`, exactly like the Add-to-Document path's resolved fields). Verified empirically both ways: a probe script confirmed `DocumentEditor`'s `DOMSerializer`-based copy already serializes `excerpt` as an attribute automatically (no fix needed there), and `clipClipboard.test.ts`'s round-trip test now asserts `clip.attrs.excerpt` survives parse.
- [x] Verify empirically whether TipTap's default HTML paste pipeline reconstructs the inline `clipBlock` node from the marker HTML with no extra code (it should, since `parseHTML` already matches); add a minimal `handlePaste` override only if the default pipeline needs a nudge (e.g. unwanted paragraph-wrapping)
  - Confirmed with no extra code needed: `clipClipboard.test.ts`'s round-trip test inserts `Before <marker-html> After` via `editor.commands.insertContent(html)` (exercises the same schema-driven `DOMParser`/`parseDOM` path a real paste does) and asserts the paragraph comes back as `['text', 'clipBlock', 'text']` with the clip's attrs intact. No `handlePaste` override was added.
- [x] Tests: copy writes both MIME entries; paste of marker HTML reconstructs an inline `clipBlock` node with correct attrs, in-flow with surrounding text
  - `clipClipboard.test.ts`: `clipBlockMarkerHtml` shape + HTML-escaping; `writeClipToClipboard`'s `ClipboardItem` branch (asserts both MIME blobs) and its `writeText` fallback; the paste round-trip test above.
  - `TranscriptViewer.test.tsx`: existing "copies the selected text" test still covers the `writeText` fallback path (jsdom has no `ClipboardItem`); new test stubs `ClipboardItem`/`clipboard.write` and asserts the written `text/html` blob contains the marker's `data-clip-block`/`transcriptId`/`videoId`/`startTokenId`/`endTokenId` attrs and excerpt text.
  - `DocumentEditor.test.tsx`: new test in the `formatting/comment bubble menu` describe block — selects "Hello" in a text selection, clicks the new `Copy` button, asserts both MIME blobs were written.
  - Clipboard-API stubbing gotcha (worth recording): stubbing the *entire* `navigator` object via `vi.stubGlobal('navigator', {...})` breaks TipTap's keymap extension, which reads `navigator.userAgent` during `Editor` construction (`isiOS`) — crashes `renderEditor()`/`renderViewer()` immediately, and since the crash happens before an inline `vi.unstubAllGlobals()` at the end of the test body, the broken global then leaks into every later test in the file. Fixed by only assigning `navigator.clipboard.write` directly (leaving the rest of `navigator` untouched) and moving all cleanup into a top-level `afterEach` so it runs even when the test fails.
- [ ] Verify manually: copy a transcript selection, paste into the document — a clip appears inline; paste the same copy into an external plain-text editor (TextEdit/Notes/Slack) — plain excerpt text appears, unaffected

## Phase E9 — Panel layout restructuring (highest-risk phase)
- [ ] `frontend/src/components/AppShell.tsx`: move `react-resizable-panels`' `Group`/`Panel`/`Separator` up here, with the routed page (`<Outlet/>`) as one `Panel` and `DocumentPanel` as its sibling `Panel` (collapsible, `collapsedSize` sized to fit the existing rail button so it's never literally zero-width)
- [ ] `frontend/src/pages/VideoWorkspace.tsx`: keep its own nested `Group` for the transcript/video split exactly as today, now as the content of `AppShell`'s "page" `Panel` instead of a plain flex container; drop the outer height wrapper, verify `h-full` flows correctly from `AppShell`'s `Panel`
- [ ] `frontend/src/features/documents/DocumentPanel.tsx`: remove manual `w-96` sizing; drive open/close via the `Panel`'s imperative `collapse()`/`expand()` API bridged from the existing `documentPanel.ts` `isOpen` store field (keep that store API unchanged)
- [ ] Verify manually: resize the document/page divider and (on a video workspace route) the transcript/video divider — both work independently; visit every other route (project view, search, chat) with the document panel open to confirm no layout breakage, since none of them currently expect to live inside a resizable `Panel`
- [ ] Tests: `AppShell` panel collapse/expand via store; existing `VideoWorkspace` layout tests still pass with the nested-`Group` structure

## Phase E10 — Video player chrome unification
- [ ] New `frontend/src/features/player/PlayerControls.tsx`: extract `VideoPlayer.tsx`'s custom control row (play/pause, ±5s, 2x) into a shared component parameterized over plain props instead of reading `usePlaybackStore` directly
- [ ] `VideoPlayer.tsx`: use `PlayerControls` wired to the global playback store (unchanged behavior)
- [ ] `frontend/src/features/documents/ClipPreviewPlayer.tsx`: drop the native `controls` attribute, use `PlayerControls` wired to local `useState` (keep its existing deliberate avoidance of the global store)
- [ ] Add an "open in workspace" affordance to `ClipPreviewPlayer` that navigates to the video's workspace route
- [ ] Tests: `ClipPreviewPlayer.test.tsx` — chrome parity assertions, promote-link navigation
- [ ] Verify manually: play a clip from the document panel for a video not currently open in the workspace — consistent chrome vs. the main workspace player; click "open in workspace" — navigates there

## Phase E11 — Docs updates
- [ ] `docs/400_database.md`: document `Comment`'s nullable `transcript_id`/new `document_id`, and the new `DocumentCommentAnchor` table
- [ ] `docs/700_backend_api.md`: add the two new document-comment routes
- [ ] `docs/800_frontend.md`: update the Application Layout diagram (nested `Group`, not a flat-peer/fallback sidebar), Comment state (document-anchored variant), Document Panel State (new insert-marker field), Player Coordination (chrome-parity note)
- [ ] `docs/1100_document_builder.md` §5 (Editor Contract) and §7 (Frontend Rendering Contract): update from "clip block card"/block-level atomic node to inline atomic node + border/tint styling; remove the `note` attr from the documented `clipBlock` attrs shape; document that clip annotations are now regular `Comment` rows anchored via `DocumentCommentAnchor.clip_node_id`
- [ ] Verify: docs read coherently end-to-end, no stale references to the retired card/note design remain
