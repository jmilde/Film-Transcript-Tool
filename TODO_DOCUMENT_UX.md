# Document Builder UX Consistency Overhaul — Implementation TODO

Persistent, git-tracked checklist for making the Document Builder feature
(`TODO_DOCUMENT_BUILDER.md`, phases D1–D8 done) consistent with the existing
transcript-editing UX: resizable panels, highlight-and-comment in document
prose, a shared "what can I do with this selection" popup, an
insertion-point marker settable from the document that survives navigating
back to the transcript, link-preserving copy/paste from transcript into
document, a uniform video player, and unified comments across transcript
and document. Stands alongside `TODO.md`, `TODO_SEMANTIC_SEARCH.md`, and
`TODO_DOCUMENT_BUILDER.md` (read `backend/CLAUDE.md` and the repo-root
`CLAUDE.md` first — same rules apply: fully typed, `mypy --strict` clean,
ruff clean, TDD, test tree mirrors source tree, explicit imports, no
`__all__`, autogenerated migrations only, `make check` green at the end of
every backend phase).

Design record: `/Users/jan/.claude/plans/i-want-to-improve-witty-glacier.md`
— read it first for full rationale on every decision below; this checklist
is the concrete execution of that plan.

Confirmed decisions (see the design record for the "why" behind each — do
not re-litigate these while implementing):
- **Comments: fully unified.** One `Comment` entity with a polymorphic
  anchor — either a transcript token range (existing `CommentRange`) or a
  document anchor (a TipTap `comment` mark on a run of prose text, or a
  `clipBlock` node's stable `nodeId`). One shared comments UI renders both,
  filtered to whichever context is active.
- **Panels: merged.** The document panel becomes a true resizable peer of
  the routed page inside one `react-resizable-panels` `Group` owned by
  `AppShell` (not a separate fixed-width sidebar). `VideoWorkspace` keeps
  its own nested transcript/video `Group` inside that outer page panel.
- **Editor:** keep TipTap WYSIWYG with markdown-style input shortcuts (no
  raw-markdown-source-view toggle). Fix that `# `/`## ` don't currently
  render as visually distinct headings (missing `@tailwindcss/typography`
  plugin registration). Add a real selection/formatting toolbar
  (bold/italic/heading levels) rather than relying on typed shortcuts
  alone — this toolbar is the same shared component used for the
  transcript's selection actions and the document's comment/clip popup.
- **Clip references: inline text, not cards.** `clipBlock` changes from a
  block-level `atom` node (rendered as a thumbnail/timecode card) to an
  **inline** `atom` node — still non-editable/read-only, excerpt still
  resolved fresh server-side, never persisted — rendered as styled inline
  text within the document's normal paragraph flow, not a boxed card.
- **Clip vs. comment visual channels don't collide.** A persistent left
  border + subtle background tint marks "this text is from source
  material" (clips); the underline decoration (violet = unresolved,
  slate = resolved, exactly as in the transcript today) is reserved
  exclusively for comments, in both transcript and document. The two
  layers stack independently — a clip with a comment on it shows both at
  once.
- **Clip "note" field is retired.** Per-clip notes fold into the unified
  comment system instead of staying a separate always-visible input — a
  note becomes a normal `Comment` row anchored via
  `DocumentCommentAnchor.clip_node_id`.
- **Insertion-point marker is a live ProseMirror plugin position, not a
  stored offset.** Never persist a raw position integer anywhere (Zustand
  included) — it goes stale on the next edit. The marker lives in the
  editor instance's own plugin state, mapped through every transaction
  exactly like the comment mark; it survives navigating to the transcript
  because `DocumentPanel`/`DocumentEditor` are already mounted persistently
  in `AppShell` and never unmount on route/focus changes.
- **Clipboard interop uses `text/html` + `text/plain`, not a custom MIME
  type** (custom `ClipboardItem` types have inconsistent cross-browser
  safelisting; `text/html`/`text/plain` are universal and `text/plain` stays
  byte-for-byte identical to today's behavior, so external paste is
  unaffected).

---

## Phase E1 — Heading typography fix
- [x] Add `@tailwindcss/typography` to `frontend/package.json` devDependencies
- [x] Add `@plugin '@tailwindcss/typography';` to `frontend/src/index.css`
- [x] Extend `DocumentEditor.test.tsx`: typing `# `/`## ` produces an `h1`/`h2` element with the typography plugin's classes applied
- [ ] Verify manually: type `# Heading` in a document, confirm it renders visually distinct from body text; check list/paragraph spacing doesn't fight the panel's existing padding

## Phase E2 — Backend: document-anchored comments schema
- [ ] `backend/app/models/comment.py`: make `Comment.transcript_id` nullable; add `Comment.document_id` (FK `documents.id`, `ondelete="CASCADE"`, indexed, nullable); add a `CheckConstraint` enforcing exactly one of `transcript_id`/`document_id` is set
- [ ] Add new model `DocumentCommentAnchor(Base, UUIDPrimaryKeyMixin, CreatedAtMixin)`: `comment_id` (FK `comments.id`, unique, indexed), `clip_node_id: str | None` (set when anchored to a `clipBlock` node's stable `nodeId`; `None` when anchored to a run of prose text, whose real anchor is the TipTap `comment` mark added in Phase E4)
- [ ] Migration: `cd backend && uv run alembic revision --autogenerate -m "0014 document comments"` — verify the `CheckConstraint` came through correctly (autogenerate sometimes needs a manual nudge for check constraints; fix the model/naming convention and regenerate rather than hand-patching DDL, per `backend/CLAUDE.md`)
- [ ] Verify `alembic upgrade head` / `alembic downgrade -1` round-trip cleanly; no backfill needed (widening an existing non-null column to nullable loses no data)
- [ ] `backend/app/schemas/comment.py`: split `CommentRead.anchor` into a discriminated union — `TranscriptCommentAnchor{kind: "transcript", transcript_id, start_token_id, end_token_id, in_time, out_time}` vs `DocumentCommentAnchorRead{kind: "document", document_id, clip_node_id, excerpt}`. Add `DocumentCommentCreate{clip_node_id: str | None, text: str}` (prose-text comments additionally need the frontend to apply the `comment` mark — see E4; `clip_node_id` set means no mark involved)
- [ ] `backend/app/services/comments.py`: add `create_document_comment(session, document, *, clip_node_id, text, user_id) -> Comment` (mirrors `create_comment`; sets `document_id` + creates the `DocumentCommentAnchor` row; does **not** touch `document.content`)
- [ ] `backend/app/services/documents.py`: add a helper resolving a document-anchored comment's excerpt at read time — reuse/generalize the existing content-tree-walking helper(s) behind `resolve_document_content` to also find `comment`-marked text spans (once E4 adds the mark) in addition to `clipBlock` nodes
- [ ] `backend/app/api/routes/comments.py`: branch `_comment_read` on `comment.transcript_id is not None` vs `document_id is not None`; add `POST /documents/{document_id}/comments` (`require_min_role(require_document_access, MembershipRole.EDITOR)` — match the existing editor-floor pattern used for transcript comment creation) and `GET /documents/{document_id}/comments` (`require_document_access`, viewer floor)
- [ ] Tests first, mirroring source tree: `tests/models/test_models.py` (nullable `transcript_id` + `document_id` construction, check-constraint violation case), `tests/services/test_comments.py` (new `create_document_comment` cases), `tests/api/routes/test_comments.py` (new document-comment route tests; **confirm every existing transcript-comment test still passes unchanged** — regression guard for the nullable-column change)
- [ ] Verify: `make check` green

## Phase E3 — Frontend: unified comments data layer
- [ ] Regenerate API client types (`npm run gen:api` or equivalent) against the new discriminated `CommentRead`
- [ ] `frontend/src/api/hooks/useComments.ts`: add `useDocumentComments(documentId)`, `useCreateDocumentComment(documentId)`
- [ ] Verify: `typecheck`/`lint` green; hook tests (MSW-backed, mirroring existing `useComments` test conventions)

## Phase E4 — Clip references as inline text + TipTap comment mark
- [ ] `frontend/src/features/documents/clipBlockNode.ts`: change `group: 'block'` → `group: 'inline', inline: true` (keep `atom: true`, `selectable: true`, `draggable: false`, same name/attrs/`insertClipBlockAt` command — no backend change needed, `resolve_document_content`'s tree-walk doesn't care about block-vs-inline); move `parseHTML`/`renderHTML` from `div[data-clip-block]` to `span[data-clip-block]`
- [ ] Remove the `note` attribute from `ClipBlockAttrs`
- [ ] Rewrite `frontend/src/features/documents/ClipBlockView.tsx` from a card layout to an inline `<span>`: render the resolved `excerpt` text itself as the visible content, styled with a persistent left-border + background tint (see decision above); drop the thumbnail/video-name/folder-path row from the inline rendering; move Play/Comment/Remove into the shared popup (E6) instead of always-visible card chrome
- [ ] Ensure inline-atom insertion always lands inside a paragraph (an inline atom can't be a direct child of `doc`): update `insertClipBlockAt`'s call sites (the pending-insert consumption effect in `DocumentEditor.tsx`, and Phase E7's marker-target insert) to wrap in a new empty paragraph when the target position is a block boundary (e.g. end of an empty document)
- [ ] New `frontend/src/features/documents/commentMark.ts`: TipTap `Mark` (not `Node`) named `comment`, carrying only `commentId` as an attribute (never `resolved`/`text` — those stay authoritative on the `Comment` row; the mark is purely a stable position reference riding ProseMirror's transaction mapping through edits/undo)
- [ ] `DocumentEditor.tsx`: register `CommentMark`; wire "select text → Comment action" to (1) POST via E3's `useCreateDocumentComment` hook, (2) on success, `editor.chain().setMark('comment', { commentId }).run()` — flows through the existing debounced-autosave/`expected_version` path. On a 409 immediately following the mark-set, retry the mark-set once against freshly-reloaded content; if that also fails, leave the comment un-highlighted (it still lists/functions via the `Comment` row) rather than inventing new conflict-resolution machinery
- [ ] Clip-anchored comments (including former "notes") skip the mark entirely: write `DocumentCommentAnchor.clip_node_id` directly from the clip node's existing stable `nodeId` attr, no document edit needed
- [ ] Add a delegated click/hover handler on `[data-comment-id]` spans and on clip nodes (event delegation at the `EditorContent` container) to open the comment thread/popup
- [ ] Compute resolved/unresolved styling (underline for comments; border+tint for clips) by diffing marks/nodes present in the doc against the loaded comments list's `resolved` field — never store `resolved` redundantly on the mark/node itself
- [ ] Tests: comment created from a selection survives a subsequent unrelated edit at an earlier position (position-mapping regression guard); the 409-orphan case; a clip renders with border+tint only, gains an underline once a comment is added, both survive a reload; inline-insert-into-empty-document paragraph-wrapping case
- [ ] Verify manually: select prose text, add a comment, reload — highlight and thread persist; edit text before the comment — mark stays attached to the right words

## Phase E5 — Extract shared `SelectionToolbar`
- [ ] New `frontend/src/features/toolbar/SelectionToolbar.tsx` + `ToolbarAction` type: `{id, icon, label, variant?, active?, onClick}[]`, a `summary: {text, timecode?}` prop, and support for the existing "swap to inline draft input" mode (the Comment button's type-then-Confirm/Cancel flow)
- [ ] Extract `TranscriptViewer.tsx`'s existing inline toolbar block (Play/Copy/Edit/Comment/Add to Document/Delete) into this component — pure extraction, zero behavior change; `TranscriptViewer` keeps its existing token-selection state and action implementations, just builds a `ToolbarAction[]` and renders `<SelectionToolbar>`
- [ ] Move/adapt existing toolbar-behavior tests
- [ ] Verify: `TranscriptViewer` behavior unchanged (existing tests pass with import-path-only diffs)

## Phase E6 — Document formatting/comment `BubbleMenu`
- [ ] Add `@tiptap/extension-bubble-menu` as a direct dependency in `frontend/package.json`
- [ ] `DocumentEditor.tsx`: use TipTap's `BubbleMenu` to render `SelectionToolbar`, with a `shouldShow` distinguishing a text selection (→ Bold/Italic/H1/H2/BulletList/Comment) from a `NodeSelection` over a `clipBlock` node (→ Play/Comment/Remove, reusing `ClipBlockView`'s play-routing logic)
- [ ] Wire the same popup to open on a click on a clip's inline span or a `[data-comment-id]` span (from E4) — one popup implementation for "what can I do with this" across selected prose, selected clips, and (via E5) selected transcript ranges
- [ ] Verify manually: select plain prose text → bubble shows Bold/Italic/H1/H2/Comment; select an inline clip → bubble shows Play/Comment/Remove

## Phase E7 — Insertion-point marker
- [ ] New `frontend/src/features/documents/insertMarker.ts`: ProseMirror plugin holding the marker position in plugin state, mapped through every transaction (`tr.mapping.map`); emits a `Decoration.widget` (thin highlighted insertion bar) at the mapped position
- [ ] `frontend/src/store/documentPanel.ts`: add `insertMarkerDocumentId: string | null` + setter (thin flag only, not a position — other components use it to render "marker is set" state without reaching into the editor instance); clear it on document switch
- [ ] `DocumentEditor.tsx`: add a "mark insert point here" affordance; change the pending-insert consumption effect (currently always inserts at `editor.state.doc.content.size`, i.e. document end) to target the marker's mapped position when set, falling back to document end otherwise. Default to **advance-after-use** (marker moves to just after the newly-inserted node); add an explicit "clear insert point" affordance to return to end-of-document behavior
- [ ] Tests: insert-at-marker lands at the correctly-mapped position after an intervening unrelated edit; marker advances after use; clears on document switch
- [ ] Verify manually: mark an insert point in the document, click into the transcript, select a range, click "Add to Document" — confirm it lands at the marked position, not document end; confirm the marker advances on repeated inserts

## Phase E8 — Clipboard interop (link-preserving copy/paste)
- [ ] `TranscriptViewer.tsx`'s Copy action (and a new equivalent for a document text/clip selection): write a `ClipboardItem` with `text/plain` unchanged (today's excerpt string) and `text/html` containing a marker element using the `data-clip-block`/`data-transcript-id`/`data-video-id`/etc. attributes `clipBlockNode.ts` already defines for its `parseHTML` (now `span[data-clip-block]` per E4), with the excerpt as the span's visible inner text. Feature-detect `ClipboardItem`/`clipboard.write`, fall back to today's `writeText` when unavailable
- [ ] Verify empirically whether TipTap's default HTML paste pipeline reconstructs the inline `clipBlock` node from the marker HTML with no extra code (it should, since `parseHTML` already matches); add a minimal `handlePaste` override only if the default pipeline needs a nudge (e.g. unwanted paragraph-wrapping)
- [ ] Tests: copy writes both MIME entries; paste of marker HTML reconstructs an inline `clipBlock` node with correct attrs, in-flow with surrounding text
- [ ] Verify manually: copy a transcript selection, paste into the document — a clip appears inline; paste the same copy into an external plain-text editor (TextEdit/Notes/Slack) — plain excerpt text appears, unaffected

## Phase E9 — Panel layout restructuring (highest-risk phase)
- [ ] `frontend/src/components/AppShell.tsx`: move `react-resizable-panels`' `Group`/`Panel`/`Separator` up here, with the routed page (`<Outlet/>`) as one `Panel` and `DocumentPanel` as its sibling `Panel` (collapsible, `collapsedSize` sized to fit the existing rail button so it's never literally zero-width)
- [ ] `frontend/src/pages/VideoWorkspace.tsx`: keep its own nested `Group` for the transcript/video split exactly as today, now as the content of `AppShell`'s "page" `Panel` instead of a plain flex container; drop the outer height wrapper, verify `h-full` flows correctly from `AppShell`'s `Panel`
- [ ] `frontend/src/features/documents/DocumentPanel.tsx`: remove manual `w-96` sizing; drive open/close via the `Panel`'s imperative `collapse()`/`expand()` API bridged from the existing `documentPanel.ts` `isOpen` store field (keep that store API unchanged)
- [ ] Verify manually: resize the document/page divider and (on a video workspace route) the transcript/video divider — both work independently; visit every other route (project view, search, chat) with the document panel open to confirm no layout breakage, since none of them currently expect to live inside a resizable `Panel`
- [ ] Tests: `AppShell` panel collapse/expand via store; existing `VideoWorkspace` layout tests still pass with the nested-`Group` structure

## Phase E10 — Video player chrome unification
- [ ] New `frontend/src/features/player/PlayerControls.tsx`: extract `VideoPlayer.tsx`'s custom control row (play/pause, ±5s, 2x) into a shared component parameterized over plain props instead of reading `usePlaybackStore` directly
- [ ] `VideoPlayer.tsx`: use `PlayerControls` wired to the global playback store (unchanged behavior)
- [ ] `frontend/src/features/documents/ClipPreviewPlayer.tsx`: drop the native `controls` attribute, use `PlayerControls` wired to local `useState` (keep its existing deliberate avoidance of the global store)
- [ ] Add an "open in workspace" affordance to `ClipPreviewPlayer` that navigates to the video's workspace route
- [ ] Tests: `ClipPreviewPlayer.test.tsx` — chrome parity assertions, promote-link navigation
- [ ] Verify manually: play a clip from the document panel for a video not currently open in the workspace — consistent chrome vs. the main workspace player; click "open in workspace" — navigates there

## Phase E11 — Docs updates
- [ ] `docs/400_database.md`: document `Comment`'s nullable `transcript_id`/new `document_id`, and the new `DocumentCommentAnchor` table
- [ ] `docs/700_backend_api.md`: add the two new document-comment routes
- [ ] `docs/800_frontend.md`: update the Application Layout diagram (nested `Group`, not a flat-peer/fallback sidebar), Comment state (document-anchored variant), Document Panel State (new insert-marker field), Player Coordination (chrome-parity note)
- [ ] `docs/1100_document_builder.md` §5 (Editor Contract) and §7 (Frontend Rendering Contract): update from "clip block card"/block-level atomic node to inline atomic node + border/tint styling; remove the `note` attr from the documented `clipBlock` attrs shape; document that clip annotations are now regular `Comment` rows anchored via `DocumentCommentAnchor.clip_node_id`
- [ ] Verify: docs read coherently end-to-end, no stale references to the retired card/note design remain
